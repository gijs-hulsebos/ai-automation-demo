# No API keys or backend environment variables are required.
# The chatbot currently runs as a local interface without a backend connection.

# Public Skillmax results only; no credentials are required for this endpoint.
SKILLMAX_PUBLIC_API_URL=https://skillmax-135087328412.europe-west4.run.app/api/public/learning-report
